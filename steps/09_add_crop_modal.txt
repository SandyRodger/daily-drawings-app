```src/components/CropModal.jsx
import Cropper from "react-easy-crop";

function CropModal({
  imageSrc,
  crop,
  zoom,
  setCrop,
  setZoom,
  onCropComplete,
  onCancel,
  onConfirm,
}) {
  if (!imageSrc) return null;

  return (
    <div className="modal-overlay">
      <div className="modal-card">
        <div className="cropper-wrapper">
          <Cropper
            image={imageSrc}
            crop={crop}
            zoom={zoom}
            aspect={3 / 4}
            onCropChange={setCrop}
            onZoomChange={setZoom}
            onCropComplete={onCropComplete}
          />
        </div>

        <div className="crop-controls">
          <label>
            Zoom
            <input
              type="range"
              min="1"
              max="3"
              step="0.1"
              value={zoom}
              onChange={(e) => setZoom(Number(e.target.value))}
            />
          </label>
        </div>

        <div className="modal-actions">
          <button type="button" onClick={onCancel} className="secondary-button">
            Cancel
          </button>
          <button type="button" onClick={onConfirm} className="primary-button">
            Use cropped image
          </button>
        </div>
      </div>
    </div>
  );
}

export default CropModal;
```

THIS CREATED SOMETHING I DIDN'T LIKE, SO THERE WAS A LOT OF CHATGPT-ING  TO GET IT RIGHT. I DIDN'T RECORD IT, FORGIVE ME, I RESOLVE TO DO BETTER.